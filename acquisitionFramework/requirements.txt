# Windows / Python 3.12 已验证的依赖组合。
# MediaPipe 0.10.14 要求 protobuf<5；TensorFlow 固定 2.18.1 与其兼容。
# 不要直接升级到会拉入 protobuf>=6 的 TensorFlow 版本。
mediapipe==0.10.14
tensorflow==2.18.1
pyriemann
scikit-learn
scipy
joblib
mne
opencv-python
mindrove
numpy<2
# PyBullet 仅用于旧版 LEAP Hand 仿真；当前 main_o3d.py 不依赖它。
pybullet
# Open3D + Torch 用于 UmeTrack FK、IK 与 LBS 蒙皮渲染。
open3d
torch
# 回归与性能验收测试。
pytest==9.1.1
